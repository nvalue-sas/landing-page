---
import type { Metric } from "@/components/trajectory/types";

interface Props {
  metrics: Metric[];
}

const { metrics } = Astro.props;
---

<div
  data-reveal
  class="animate-delay-150 mt-10 grid grid-cols-3 gap-4 border-y border-secondary-300/70 py-6 opacity-0 dark:border-white/10"
>
  {
    metrics.map(([value, label]) => (
      <div>
        <div class="text-3xl font-semibold text-cyan-600 sm:text-4xl dark:text-cyan-400">
          {value}
        </div>
        <div class="mt-1 font-mono text-[10px] tracking-[0.18em] text-secondary-500 uppercase dark:text-secondary-400">
          {label}
        </div>
      </div>
    ))
  }
</div>